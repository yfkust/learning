

## SKills

#### 文献：nature-academic-search、nature-downloader、nature-reader、nature-paper-card、paper-comparator

#### 深度理解：paper-method-explainer、paper-experiment-analyzer

#### 写作与投稿：nature-writing、nature-polishing、nature-citation、nature-statistics、nature-reviewer、nature-shared

#### 绘图：nature-figure

## 一、screen 命令：

| **1. 创建新会话**           | `screen -S exp_name`            | 创建一个名为 `exp_name` 的虚拟终端。进入后直接跑你的 `python train.py`。 |
| --------------------------- | ------------------------------- | ------------------------------------------------------------ |
| **2. 暂时离开（后台运行）** | 按 **`Ctrl+a`**，然后按 **`d`** | 实验会在后台继续跑，你回到原始终端。**（关键操作：分离会话）** |
| **3. 恢复会话**             | `screen -r exp_name`            | 重连 SSH 后，用此命令切回实验界面，无缝衔接。                |

| 操作                       | 命令                    | 说明                                         |
| :------------------------- | :---------------------- | :------------------------------------------- |
| **列出所有会话**           | `screen -ls`            | 显示所有后台会话及其 ID 或名称。             |
| **恢复唯一的会话**         | `screen -r`             | 如果只有一个后台会话，直接输入即可恢复。     |
| **强制恢复（踢掉旧连接）** | `screen -d -r exp_name` | 当你恢复时报错 "Attached" 时使用，强制接管。 |
| **彻底结束会话**           | 在会话内输入 `exit`     | 退出当前 screen 并关闭该会话（释放资源）。   |

#### （1）创建多个窗口

你可以在一个 screen 会话里开多个终端窗口，像浏览器标签页一样切换，这样不用开多个 SSH 窗口。

- **新建窗口**：按 `Ctrl+a`，然后按 `c`（create）
- **切换窗口**：按 `Ctrl+a`，然后按 `n`（next）或 `p`（previous）
- **查看窗口列表**：按 `Ctrl+a`，然后按 `"`（双引号）

#### （2）复制与滚动（查看报错信息）

Screen 默认不支持鼠标滚轮翻页，需要用“复制模式”：

- 按 `Ctrl+a`，然后按 `[`（左方括号）进入复制模式。
- 使用**方向键（↑↓）**或 **PageUp/PageDown** 翻看历史输出。
- 看完后按 `Esc` 键退出复制模式。

> **优化小技巧**：如果你想用鼠标滚轮，可以在 `~/.screenrc` 文件中添加一行 `termcapinfo xterm* ti@:te@`，然后重新打开 screen，鼠标滚轮就能用了。

## 二、nohup:

nohup python train.py > train.log 2>&1 &

nohup          忽略 SSH 断开产生的挂断信号
python train.py
> train.log    标准输出写入 train.log
> 2>&1           错误输出也写入 train.log
> &              放到后台执行

启动之后建议执行：

```
echo $!
```

会得到进程 PID，例如：

```
283741
```

之后 SSH 断了、重新登录，可以检查：

```
ps -p 283741
ps -f -p 
```

或者：

```
ps aux | grep train.py
```

### 查看实验输出

实时查看日志：

```
tail -f train.log
```

### 如果在指定 GPU 上训练

比如：

```
CUDA_VISIBLE_DEVICES=0 python train.py
```

可以：

```
nohup env CUDA_VISIBLE_DEVICES=0 python train.py > train.log 2>&1 &
```

或者一般这样也可以：

```
CUDA_VISIBLE_DEVICES=0 nohup python train.py > train.log 2>&1 &
```

然后：

```
echo $!
```

如果你希望**启动命令执行完马上返回终端，同时明确脱离当前 shell**，还可以：

```
nohup python train.py > train.log 2>&1 &
disown
```

### 推荐组合：

```
nohup python train.py > train.log 2>&1 &
echo $!
disown
```

### 更方便的写法

比如实验叫 `exp01`：

```
mkdir -p logs
nohup python train.py > logs/exp01.log 2>&1 &
echo $! > logs/exp01.pid
disown
```

以后重新 SSH 登录：

```
# 看进程
ps -fp $(cat logs/exp01.pid)

# 看日志
tail -f logs/exp01.log

# 停止实验
kill $(cat logs/exp01.pid)
```

## 三、scp命令

scp [选项] 原路径 目标路径

服务器路径：用户名@服务器IP:/路径/

常见命令：

| 场景                | 命令                                                  |
| ------------------- | ----------------------------------------------------- |
| 本地文件 → 服务器   | `scp file.txt user@192.168.1.100:/home/user/`         |
| 服务器文件 → 本地   | `scp user@192.168.1.100:/home/user/file.txt ./`       |
| 本地文件夹 → 服务器 | `scp -r data/ user@192.168.1.100:/home/user/`         |
| 服务器文件夹 → 本地 | `scp -r user@192.168.1.100:/home/user/data/ ./`       |
| 指定 SSH 端口       | `scp -P 2222 file.txt user@192.168.1.100:/home/user/` |
| 传多个文件          | `scp a.txt b.txt user@192.168.1.100:/home/user/`      |
| 保留时间戳/权限     | `scp -p file.txt user@192.168.1.100:/home/user/`      |
| 压缩传输            | `scp -C file.tar user@192.168.1.100:/home/user/`      |

## 四、开启bash上建命令检索

配置：

```
nano ~/.inputrc
```

写入：

```
"\e[A": history-search-backward
"\e[B": history-search-forward
```

然后：

```
bind -f ~/.inputrc
```

## 五、终端：codex 走 websockets

#Clash proxy config

PROXY_HTTP="http://127.0.0.1:7897"
PROXY_SOCKS="socks5://127.0.0.1:7897"

proxy_on() {
    export http_proxy="$PROXY_HTTP"
    export https_proxy="$PROXY_HTTP"
    export HTTP_PROXY="$PROXY_HTTP"
    export HTTPS_PROXY="$PROXY_HTTP"

    export all_proxy="$PROXY_SOCKS"
    export ALL_PROXY="$PROXY_SOCKS"
    
    echo "终端代理已开启。"
}

proxy_off() {
    unset http_proxy https_proxy all_proxy
    unset HTTP_PROXY HTTPS_PROXY ALL_PROXY
    echo "终端代理已关闭。"
}

proxy_status() {
    echo "http_proxy=$http_proxy"
    echo "https_proxy=$https_proxy"
    echo "all_proxy=$all_proxy"
    echo "HTTP_PROXY=$HTTP_PROXY"
    echo "HTTPS_PROXY=$HTTPS_PROXY"
    echo "ALL_PROXY=$ALL_PROXY"
}

codex_proxy() {
    env \
      ALL_PROXY="$PROXY_SOCKS" \
      all_proxy="$PROXY_SOCKS" \
      HTTPS_PROXY="$PROXY_HTTP" \
      https_proxy="$PROXY_HTTP" \
      HTTP_PROXY="$PROXY_HTTP" \
      http_proxy="$PROXY_HTTP" \
      codex "$@"
}

#新终端默认关闭代理

unset http_proxy https_proxy all_proxy
unset HTTP_PROXY HTTPS_PROXY ALL_PROXY



## 六、NVIDIA

nvidia-smi -i 1 -l 5

top

watch -n 1 nvidia-smi



## 七、nano

挑转到行首：Ctrl + A

跳转到行尾：Ctrl + E

**跳转到末尾：ALT + /** 

跳转到开头：ALT + \

**删行：alt + k**

**粘贴内容**：`Ctrl + U`（可以把刚刚 `Ctrl + K` 剪切的内容粘回来）。

多行删除：ALT + A

^代表Ctrl；M代表Alt

跳转行：ctrl + shift + -

显示行号：nano -l 文件

在 `nano` 中，通常的 `Ctrl+C`/`V` 是无效的（它们被用于显示位置和换行）。

- **剪切整行（整行删除）**：`Ctrl + K`（可以连续按，切掉多行）。
- **粘贴内容**：`Ctrl + U`（可以把刚刚 `Ctrl + K` 剪切的内容粘回来）。
- **多行自由复制/剪切（标记模式）**：
  1. 将光标移到起点，按下 **`Alt + A`**（开启标记，屏幕底部会显示 `Mark Set`）。
  2. 移动方向键选中你想要的文本块。
  3. 按 **`Alt + 6`** 复制选中的文本（或按 `Ctrl + K` 剪切选中的文本）。
  4. 移动光标到目标位置，按 **`Ctrl + U`** 粘贴。
- 撤销与反撤销

- **撤销上一步操作（Undo）**：`Alt + U`
- **重做/反撤销（Redo）**：`Alt + E`
- 显示行号

- **临时显示/隐藏行号**：`Alt + N`（再次按下可关闭）。

- 如果想**永久显示行号**，可以修改或创建你的个人配置文件：

  bash

  ```
  echo "set linenumbers" >> ~/.nanorc
  ```

  Use code with caution.

  









